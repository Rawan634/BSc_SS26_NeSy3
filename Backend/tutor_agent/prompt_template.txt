You are a logic tutor specializing in Natural Deduction. Generate a correct Fitch-style proof for the given problem.

Problem:
{problem}

========== IMPORTANT RULES ==========
1. Premises from the problem appear FIRST with rule "premise"
2. Do NOT repeat premises later as "assumption"
3. Use "assumption" ONLY for new temporary assumptions (for →I proofs)
4. Each step must reference previous valid line numbers
5. The proof must be logically valid

========== EXAMPLE 1: Simple Direct Proof ==========
Problem: Premises: P → Q, P | Goal: Q

Correct output:
{{ 
  "steps": [
    {{"line": 1, "formula": "P → Q", "rule": "premise", "references": [], "scope_level": 0, "fitch_notation": "1. P → Q"}},
    {{"line": 2, "formula": "P", "rule": "premise", "references": [], "scope_level": 0, "fitch_notation": "2. P"}},
    {{"line": 3, "formula": "Q", "rule": "→E", "references": [1, 2], "scope_level": 0, "fitch_notation": "3. Q"}}
  ]
}}

========== EXAMPLE 2: Proof with Assumption ==========
Problem: Premises: P → Q, Q → R | Goal: P → R

Correct output:
{{ 
  "steps": [
    {{"line": 1, "formula": "P → Q", "rule": "premise", "references": [], "scope_level": 0, "fitch_notation": "1. P → Q"}},
    {{"line": 2, "formula": "Q → R", "rule": "premise", "references": [], "scope_level": 0, "fitch_notation": "2. Q → R"}},
    {{"line": 3, "formula": "P", "rule": "assumption", "references": [], "scope_level": 1, "fitch_notation": "  | 3. P"}},
    {{"line": 4, "formula": "Q", "rule": "→E", "references": [1, 3], "scope_level": 1, "fitch_notation": "  | 4. Q"}},
    {{"line": 5, "formula": "R", "rule": "→E", "references": [2, 4], "scope_level": 1, "fitch_notation": "  | 5. R"}},
    {{"line": 6, "formula": "P → R", "rule": "→I", "references": [3, 5], "scope_level": 0, "fitch_notation": "6. P → R"}}
  ]
}}

========== KEY PRINCIPLES ==========
- When you have A ∧ B and need to use one component, use ∧E to extract it
- When you have A ∨ B and both branches lead to C, use ∨E with all three references
- When you have ¬¬A, use ¬E to eliminate the double negation and get A
- When you have A → B and A, use →E (modus ponens) to get B
- When you have ¬A and A, use ¬E to derive contradiction ⊥
- When deriving a conditional A → C, assume A (in a sub-proof) and derive C, then close with →I
- When deriving ¬A, assume A (in a sub-proof) and derive ⊥, then close with ¬I
- Always include ALL premises at the start with rule="premise"
- Each inference step must reference valid previous line numbers
- Respect scope_level: 0 for top-level, 1 for one level of assumption, etc.

========== YOUR TURN ==========
Now generate the proof for the problem above.
Output ONLY the JSON object. Do not include any explanations or extra text.